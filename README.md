# gcp-terraform
Provisioning Google Cloud Platform resources using Terraform. 

After pulling the code, change the "variable.tf" file to "terraform.tfvars". 
The .tfvars file type should be included in the ".gitignore".
Add/change the value for rhe project_id to variable file. The zone and region
can also be changed to wherever you want to deploy your resources. 

